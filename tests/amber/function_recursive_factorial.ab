fun fact(n: Num): Num {
    if n <= 1 {
        let result = 1
        return result
    } else {
        let prev = n - 1
        let sub = fact(prev)
        let result = n * sub
        return result
    }
}
echo(fact(5))
