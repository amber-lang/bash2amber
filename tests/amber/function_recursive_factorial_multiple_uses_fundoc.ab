fun fact(n: Int): Int {
    if n <= 1 {
        return 1
    } else {
        let prev = n - 1
        let result = fact(prev)
        let sub = result
        return n * sub
    }
}
let result = fact(5)
echo("Here is a result: {result}")
echo("The result is: {result}")
